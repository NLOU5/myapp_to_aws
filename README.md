# myapp_to_aws
Default Template for Node JS + React JS + Express js + Docker + Docker Compose


1- git clone https:github.com/NLOU5/myapp.git 
2- Install node modules on the back-end and front-end by using npm install -y

P.S check npm modules presence for the front-end/ and eventually install them with npm install
